{
  "name": "etymolingua-functions",
  "private": true,
  "type": "module",
  "main": "lib/functions/src/index.js",
  "scripts": {
    "build": "tsc",
    "serve": "npm run build && firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions",
    "lint": "tsc --noEmit"
  },
  "engines": {
    "node": "20"
  },
  "dependencies": {
    "@google/genai": "^1.40.0",
    "firebase-admin": "^13.6.0",
    "firebase-functions": "^6.4.0"
  },
  "devDependencies": {
    "typescript": "~5.8.2"
  }
}
